// Global Cypress E2E support entrypoint.
// Keep this file side-effect free: authenticated tests must source credentials
// from Cypress.env and must never embed operational credentials in the repo.

export {}
