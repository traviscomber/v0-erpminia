'use client';

import Link from 'next/link';
import useSWR from 'swr';
import { AlertTriangle, ArrowRight, CheckCircle2, Clock3, Eye, GitBranch, RefreshCw, ShieldCheck } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

type Severity = 'critical' | 'warning' | 'info';
type Confidence = 'high' | 'medium' | 'low';
type BaseDecision = { id:string; category:'maintenance'|'preventive'|'inventory'|'documents'|'finance'; severity:Severity; title:string; description:string; responsibleArea:string; dueDate:string|null; amount:number|null; href:string; sourceId:string };
type ExecutiveResponse = { summary?:unknown; decisions?:BaseDecision[]; generatedAt?:string };
type HealthStatus = 'healthy'|'watch'|'critical'|'unknown';
type HealthMetric = { label:string; value:string|number|null; ageDays?:number|null };
type HealthDomain = { key:string; label:string; status:HealthStatus; headline:string; metrics:HealthMetric[]; action:string; href:string };
type HealthResponse = { overall:HealthStatus; domains:HealthDomain[]; policy:{ freshnessWatchDays:number; freshnessCriticalDays:number }; generatedAt:string };
type MineRow = { key:string; mineName:string; actualTons:number; expectedTonsToCutoff:number; observedVsExpectedPct:number|null; attention:'alert'|'watch'|'ok'|'no_comparison' };
type ProductionResponse = { mines:MineRow[]; transportThrough:string|null; plantThrough:string|null; drillingFreshness?:{ max_date?:string|null }|null };
type RootCauseChain = { id:string; assetId:string|null; assetCode:string|null; assetName:string|null; equipmentStatus:string|null; observation:string|null; breakAt:'work_order'|'materials'|'supply_need'|'procurement_request'|'purchase_order'|'receipt'|'resolved'; action:string; evidenceLevel:'operational_only'|'partial'|'linked'; workOrder:{ id:string; number:string; title:string; status:string; priority:string|null; rootCause:string|null }|null; materials:{ count:number; shortage:number }; procurement:{ id:string; requestNumber:string; status:string }|null; purchaseOrders:Array<{ id:string; number:string; status:string; received:boolean }> };
type RootCauseResponse = { chains:RootCauseChain[]; generatedAt:string };
type Decision = { id:string; category:string; severity:Severity; title:string; description:string; responsibleArea:string; href:string; dueDate?:string|null; amount?:number|null; confidence:Confidence; confidenceReason:string; cause?:string; breakPoint?:string; nextAction?:string };
type ActionState = { source_key:string; status:'pending'|'read'|'snoozed'; snoozed_until:string|null; updated_at:string };
type ActionStateResponse = { states:ActionState[] };

const fetcher = async (url:string) => { const response=await fetch(url,{credentials:'include',cache:'no-store'}); const payload=await response.json().catch(()=>null); if(!response.ok) throw new Error(payload?.error||'No se pudo cargar la fuente'); return payload; };
const severityRank:Record<Severity,number>={critical:0,warning:1,info:2};
const confidenceRank:Record<Confidence,number>={high:0,medium:1,low:2};
const statusConfidence:Record<HealthStatus,Confidence>={healthy:'high',watch:'medium',critical:'low',unknown:'low'};
const confidenceLabel:Record<Confidence,string>={high:'Confianza alta',medium:'Con cautela',low:'Confianza baja'};
const severityLabel:Record<Severity,string>={critical:'Crítica',warning:'Atención',info:'Seguimiento'};
const breakLabel:Record<RootCauseChain['breakAt'],string>={work_order:'Falta OT',materials:'Falta evidencia de repuestos',supply_need:'Falta necesidad de abastecimiento',procurement_request:'Falta promoción a Compras',purchase_order:'Falta OC operacional',receipt:'Falta recepción',resolved:'Cadena cerrada'};
const EXECUTIVE_LIMIT=10;
const actionKey=(decisionId:string)=>`executive:${decisionId}`;
function n(value:number,digits=0){return Number(value||0).toLocaleString('es-CL',{maximumFractionDigits:digits});}
function productionFreshness(domain?:HealthDomain){const transport=domain?.metrics?.find((m)=>m.label.startsWith('Transporte')); return {status:(domain?.status||'unknown') as HealthStatus,ageDays:transport?.ageDays??null};}
function confidenceReason(domain?:HealthDomain){if(!domain)return'Sin evaluación de Data Health disponible.'; if(domain.status==='healthy')return'Data Health: fuente dentro de ventana de frescura y sin bloqueo estructural.'; if(domain.status==='watch')return`Data Health: usar con cautela. ${domain.headline}`; if(domain.status==='critical')return`Data Health: lectura limitada. ${domain.headline}`; return'Data Health: estado de fuente desconocido.';}
function rootCauseSeverity(chain:RootCauseChain):Severity{const status=String(chain.equipmentStatus||'').toLowerCase(); const priority=String(chain.workOrder?.priority||'').toLowerCase(); if(status.includes('fuera de servicio')||priority==='critical'||priority==='critica'||priority==='crítica')return'critical'; return'warning';}
function groupKey(chain:RootCauseChain){return `${chain.assetId||chain.assetCode||chain.id}:${chain.breakAt}`;}
function dominant(items:Decision[],selector:(decision:Decision)=>string){const counts=new Map<string,number>(); for(const item of items){const key=selector(item)||'Sin asignar'; counts.set(key,(counts.get(key)||0)+1);} const entry=[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0]))[0]; return entry?{label:entry[0],count:entry[1]}:{label:'Sin presión',count:0};}

export default function DecisionCenterV3(){
 const base=useSWR<ExecutiveResponse>('/api/dashboard/ia-operacional',fetcher,{revalidateOnFocus:false});
 const health=useSWR<HealthResponse>('/api/data-quality/health',fetcher,{revalidateOnFocus:false});
 const production=useSWR<ProductionResponse>('/api/produccion/inteligencia',fetcher,{revalidateOnFocus:false});
 const rootCause=useSWR<RootCauseResponse>('/api/intelligence/root-cause',fetcher,{revalidateOnFocus:false});
 const actionStates=useSWR<ActionStateResponse>('/api/actions/state',fetcher,{revalidateOnFocus:false});
 const loading=base.isLoading||health.isLoading||production.isLoading||rootCause.isLoading;
 const error=base.error||health.error||production.error||rootCause.error;
 const stateError=actionStates.error;
 const domains=health.data?.domains||[]; const productionHealth=domains.find((d)=>d.key==='production'); const transportFreshness=productionFreshness(productionHealth);
 const openChains=(rootCause.data?.chains||[]).filter((chain)=>chain.breakAt!=='resolved');
 const linkedRootWorkOrderIds=new Set(openChains.flatMap((chain)=>chain.workOrder?[chain.workOrder.id]:[]));
 const chainGroups=new Map<string,RootCauseChain[]>();
 for(const chain of openChains){const key=groupKey(chain); chainGroups.set(key,[...(chainGroups.get(key)||[]),chain]);}
 const suppressedMaintenance=(base.data?.decisions||[]).filter((item)=>item.category==='maintenance'&&linkedRootWorkOrderIds.has(item.sourceId)).length;
 const consolidatedChains=openChains.length-chainGroups.size;
 const decisions:Decision[]=[];
 for(const item of base.data?.decisions||[]){if(item.category==='maintenance'&&linkedRootWorkOrderIds.has(item.sourceId))continue; decisions.push({...item,category:item.category==='maintenance'||item.category==='preventive'?'Mantención':item.category==='inventory'?'Inventario':item.category==='documents'?'Documentos':'Finanzas',confidence:'high',confidenceReason:'Registro canónico vigente del sistema; la decisión no depende de una fuente operacional de Producción atrasada.'});}
 for(const domain of domains.filter((d)=>d.status==='critical'||d.status==='watch')) decisions.push({id:`data-health-${domain.key}`,category:'Calidad de datos',severity:domain.status==='critical'?'critical':'warning',title:`${domain.label}: ${domain.headline}`,description:domain.action,responsibleArea:domain.label,href:domain.href,confidence:'high',confidenceReason:'La excepción describe la calidad/frescura de la fuente misma; no interpreta desempeño operacional.'});
 const prodConfidence=statusConfidence[productionHealth?.status||'unknown']; const prodReason=confidenceReason(productionHealth);
 for(const mine of production.data?.mines||[]){if(mine.attention!=='alert'&&mine.attention!=='watch')continue; const gap=Math.max(0,mine.expectedTonsToCutoff-mine.actualTons); if(transportFreshness.status==='critical'||transportFreshness.status==='unknown')continue; const rawSeverity:Severity=mine.attention==='alert'?'critical':'warning'; const severity:Severity=transportFreshness.status==='watch'?(rawSeverity==='critical'?'warning':'info'):rawSeverity; decisions.push({id:`production-${mine.key}`,category:'Producción',severity,title:`${mine.mineName}: ${mine.attention==='alert'?'ritmo bajo':'ritmo en vigilancia'}`,description:`${n(mine.actualTons,1)} t observadas vs ${n(mine.expectedTonsToCutoff,1)} t esperadas al corte; brecha ${n(gap,1)} t.`,responsibleArea:'Producción',href:'/dashboard/produccion/inteligencia',confidence:prodConfidence,confidenceReason:prodReason});}
 for(const [key,group] of chainGroups){
  const primary=[...group].sort((a,b)=>severityRank[rootCauseSeverity(a)]-severityRank[rootCauseSeverity(b)])[0];
  const causes=[...new Set(group.map((chain)=>chain.workOrder?.rootCause?.trim()||chain.observation?.trim()).filter((value):value is string=>Boolean(value)))];
  const workOrders=[...new Map(group.filter((chain)=>chain.workOrder).map((chain)=>[chain.workOrder!.id,chain.workOrder!])).values()];
  const confidence:Confidence=group.some((chain)=>chain.evidenceLevel==='linked')?'high':'medium';
  const description=group.length>1?`${group.length} observaciones del mismo activo convergen en ${breakLabel[primary.breakAt].toLowerCase()}.`:primary.workOrder?`${primary.workOrder.number} · ${primary.workOrder.title}. La cadena está acreditada hasta ${breakLabel[primary.breakAt].toLowerCase()}.`:'Observación operacional sin OT vinculada. La cadena se detiene antes de Mantención.';
  decisions.push({id:`root-cause-group-${key}`,category:'Cadena operacional',severity:rootCauseSeverity(primary),title:`${primary.assetCode||primary.assetName||'Equipo'}: ${breakLabel[primary.breakAt]}`,description,responsibleArea:primary.breakAt==='work_order'||primary.breakAt==='materials'?'Mantención':primary.breakAt==='supply_need'?'Mantención / Bodega':'Compras',href:workOrders.length===1?`/dashboard/mantenimiento/ordenes-trabajo/${workOrders[0].id}`:'/dashboard/decisiones/causa-raiz',confidence,confidenceReason:confidence==='high'?'Cadena respaldada por vínculos canónicos explícitos entre OT y abastecimiento.':'Evidencia operacional consolidada por activo y etapa; no se atribuye una causa posterior sin vínculo canónico.',cause:causes.slice(0,2).join(' · ')||'Existe una observación operacional, pero la causa técnica todavía no está acreditada.',breakPoint:breakLabel[primary.breakAt],nextAction:primary.action});
 }
 const unique=new Map<string,Decision>(); for(const decision of decisions)unique.set(decision.id,decision);
 const ordered=Array.from(unique.values()).sort((a,b)=>severityRank[a.severity]-severityRank[b.severity]||confidenceRank[a.confidence]-confidenceRank[b.confidence]||a.title.localeCompare(b.title));
 const stateByKey=new Map((actionStates.data?.states||[]).map((state)=>[state.source_key,state]));
 const now=Date.now();
 const deferred=ordered.filter((decision)=>{if(decision.severity==='critical')return false; const state=stateByKey.get(actionKey(decision.id)); return state?.status==='snoozed'&&Boolean(state.snoozed_until)&&new Date(state.snoozed_until!).getTime()>now;});
 const active=ordered.filter((decision)=>!deferred.some((item)=>item.id===decision.id));
 const visible=active.slice(0,EXECUTIVE_LIMIT); const hiddenCount=Math.max(0,active.length-visible.length); const consolidatedCount=suppressedMaintenance+consolidatedChains;
 const critical=active.filter((d)=>d.severity==='critical').length; const lowConfidence=active.filter((d)=>d.confidence==='low').length; const withRootCause=active.filter((d)=>d.breakPoint).length; const reviewed=active.filter((d)=>stateByKey.get(actionKey(d.id))?.status==='read').length;
 const areaFocus=dominant(active,(d)=>d.responsibleArea); const typeFocus=dominant(active,(d)=>d.category);
 const refresh=async()=>{await Promise.all([base.mutate(),health.mutate(),production.mutate(),rootCause.mutate(),actionStates.mutate()]);};
 const setState=async(decision:Decision,status:'pending'|'read'|'snoozed')=>{if(decision.severity==='critical'&&status==='snoozed')return; const response=await fetch('/api/actions/state',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({sourceKey:actionKey(decision.id),status})}); if(!response.ok)throw new Error('No se pudo guardar el estado de la decisión'); await actionStates.mutate();};
 return <div className="space-y-6">
  <section className="flex flex-col gap-4 border-b pb-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-sm font-medium text-muted-foreground">Gerencia · decisión operacional</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Centro ejecutivo de decisiones</h1><p className="mt-2 max-w-3xl text-sm text-muted-foreground">Máximo 10 decisiones prioritarias, consolidadas por activo y etapa causal. Las decisiones no críticas pueden revisarse o posponerse 24 h; una crítica nunca se oculta.</p></div><div className="flex flex-wrap gap-2"><Button asChild variant="outline"><Link href="/dashboard/decisiones/causa-raiz"><GitBranch className="mr-2 h-4 w-4"/>Causa raíz</Link></Button><Button asChild variant="outline"><Link href="/dashboard/calidad-datos/salud"><ShieldCheck className="mr-2 h-4 w-4"/>Data Health</Link></Button><Button variant="outline" onClick={()=>void refresh()} disabled={loading}><RefreshCw className="mr-2 h-4 w-4"/>Actualizar</Button></div></section>
  {error?<Card className="border-destructive/30"><CardContent className="p-6 text-sm text-destructive">No fue posible construir la bandeja ejecutiva completa. No se muestran decisiones parciales como si fueran completas.</CardContent></Card>:null}
  {stateError?<Card className="border-dashed"><CardContent className="p-4 text-sm text-muted-foreground">No se pudo cargar tu estado personal de revisión. Las decisiones siguen visibles y sin filtrar.</CardContent></Card>:null}
  <section className="grid gap-px overflow-hidden rounded-lg border bg-border sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8"><Metric label="Decisiones activas" value={loading?'—':n(active.length)} detail="Después de consolidar"/><Metric label="Críticas" value={loading?'—':n(critical)} detail="Siempre visibles"/><Metric label="Área con más presión" value={loading?'—':areaFocus.label} detail={loading?'Calculando':`${n(areaFocus.count)} decisión(es)`}/><Metric label="Señal dominante" value={loading?'—':typeFocus.label} detail={loading?'Calculando':`${n(typeFocus.count)} decisión(es)`}/><Metric label="Revisadas" value={loading?'—':n(reviewed)} detail="Estado personal"/><Metric label="Pospuestas" value={loading?'—':n(deferred.length)} detail="Retornan en 24 h"/><Metric label="Señales consolidadas" value={loading?'—':n(consolidatedCount)} detail="Duplicados absorbidos"/><Metric label="Confianza baja" value={loading?'—':n(lowConfidence)} detail={`${withRootCause} con cadena causal`}/></section>
  <Card className="shadow-none"><CardHeader className="flex-row items-start justify-between space-y-0"><div><CardTitle className="text-lg">Prioridades ejecutivas</CardTitle><p className="mt-1 text-sm text-muted-foreground">Una decisión por problema operativo acreditado; revisar no resuelve la causa y sólo ordena tu bandeja.</p></div><Badge variant={critical?'destructive':'outline'}>{critical?`${critical} crítica(s)`:'Sin críticas'}</Badge></CardHeader><CardContent className="p-0">{loading?<div className="space-y-2 p-5">{Array.from({length:5}).map((_,i)=><div key={i} className="h-28 animate-pulse rounded-lg bg-muted"/>)}</div>:visible.length===0?<div className="p-10 text-center"><CheckCircle2 className="mx-auto h-6 w-6 text-muted-foreground"/><p className="mt-3 font-medium">Sin decisiones activas</p></div>:<><div className="divide-y border-t">{visible.map((decision)=>{const personal=stateByKey.get(actionKey(decision.id)); return <div key={decision.id} className="grid gap-4 p-4 lg:grid-cols-[minmax(0,1fr)_230px_auto] lg:items-center"><div className="min-w-0"><div className="flex flex-wrap gap-2"><Badge variant={decision.severity==='critical'?'destructive':'outline'}>{severityLabel[decision.severity]}</Badge><Badge variant="secondary">{decision.category}</Badge><Badge variant={decision.confidence==='low'?'destructive':'outline'}>{confidenceLabel[decision.confidence]}</Badge>{decision.breakPoint?<Badge variant="outline">{decision.breakPoint}</Badge>:null}{personal?.status==='read'?<Badge variant="outline"><Eye className="mr-1 h-3 w-3"/>Revisada</Badge>:null}</div><p className="mt-2 font-medium">{decision.title}</p><p className="mt-1 text-sm text-muted-foreground">{decision.description}</p>{decision.cause?<p className="mt-2 text-sm"><span className="font-medium">Causa/evidencia:</span> {decision.cause}</p>:null}{decision.nextAction?<p className="mt-1 text-sm"><span className="font-medium">Siguiente acción:</span> {decision.nextAction}</p>:null}<p className="mt-2 text-xs text-muted-foreground">{decision.confidenceReason}</p></div><div className="text-sm"><p className="text-xs text-muted-foreground">Responsable</p><p className="mt-1 font-medium">{decision.responsibleArea}</p>{decision.breakPoint?<><p className="mt-3 text-xs text-muted-foreground">Punto de corte</p><p className="mt-1 font-medium">{decision.breakPoint}</p></>:null}</div><div className="flex flex-wrap justify-start gap-2 lg:justify-end"><Button asChild size="sm" variant={decision.severity==='critical'?'default':'outline'}><Link href={decision.href}>Resolver <ArrowRight className="ml-2 h-4 w-4"/></Link></Button>{personal?.status==='read'?<Button size="sm" variant="ghost" onClick={()=>void setState(decision,'pending')}>Pendiente</Button>:<Button size="sm" variant="ghost" onClick={()=>void setState(decision,'read')}><Eye className="mr-1 h-4 w-4"/>Revisada</Button>}{decision.severity!=='critical'?<Button size="sm" variant="ghost" onClick={()=>void setState(decision,'snoozed')}><Clock3 className="mr-1 h-4 w-4"/>24 h</Button>:null}</div></div>;})}</div>{hiddenCount>0?<div className="border-t bg-muted/20 px-5 py-4 text-sm text-muted-foreground">{hiddenCount} decisión(es) adicionales permanecen activas en sus módulos de detalle.</div>:null}{deferred.length>0?<div className="border-t bg-muted/20 px-5 py-4 text-sm text-muted-foreground">{deferred.length} decisión(es) no críticas están pospuestas para este usuario. Las críticas no pueden posponerse.</div>:null}</>}</CardContent></Card>
  {productionHealth?.status==='critical'?<Card className="border-dashed"><CardContent className="flex gap-3 p-4 text-sm"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0"/><div><p className="font-medium">Señales de ritmo de Producción retenidas</p><p className="mt-1 text-muted-foreground">La fuente de Producción está en estado crítico de Data Health. Las desviaciones por mina no se escalan como fallas operacionales hasta actualizar la evidencia.</p></div></CardContent></Card>:null}
 </div>;
}
function Metric({label,value,detail}:{label:string;value:string;detail:string}){return <div className="min-w-0 bg-card p-4"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-2 truncate text-xl font-semibold" title={value}>{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div>;}
